type Query {
  getMessages: [Message!]!
}

type Mutation {
  sendMessage(message: String!): Message!
}

type Subscription {
  newMessage: Message!
}

type Message {
  content: String!
  timestamp: String!
}
