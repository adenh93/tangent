type Query {
  getMessages: [Message!]!
}

type Mutation {
  sendMessage(message: String!): Message!
}

type Subscription {
  message: Message!
}

type Message {
  content: String!
  timestamp: String!
}
